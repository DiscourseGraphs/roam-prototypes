{
  "name": "roam-prototypes",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "prototypes/*"
  ],
  "scripts": {
    "build": "node scripts/build.mjs",
    "prepare:artifacts": "node scripts/prepare-artifacts.mjs",
    "publish": "node scripts/publish.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@vercel/blob": "^2.8.0"
  },
  "engines": {
    "node": ">=22"
  }
}
